import { Component } from '@angular/core';

@Component ({
    moduleId: module.id,
    selector: 'books-app',
    templateUrl: 'app.component.html'
})

export class AppComponent {
    title = 'My Library';
}